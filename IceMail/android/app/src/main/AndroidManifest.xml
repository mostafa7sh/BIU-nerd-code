<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="GmailApp"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.DayNight.NoActionBar"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:ignore="ExtraText"
        tools:targetApi="24">

        <activity android:name=".view.CreateLabelActivity" />

        <activity android:name=".view.LabelActivity" />

        <activity android:name=".view.ViewMailActivity" />

        <activity android:name=".view.ComposeActivity" />

        <activity android:name=".view.InboxActivity" />

        <activity
            android:name=".view.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".view.RegisterActivity" />


    </application>

</manifest>
